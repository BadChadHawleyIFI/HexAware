' Code-behind stub for the BillingPage.aspx markup fixture.
Public Class BillingPage
    Public Sub btnSubmit_Click(sender As Object, e As EventArgs)
        ' Server postback target for OnClick="btnSubmit_Click" in the markup.
    End Sub

    Public Shared Sub GetTaxAjax()
        ' AJAX target for PageMethods.GetTaxAjax() in the inline script.
    End Sub
End Class
